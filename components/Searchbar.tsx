import { Input } from "./ui/input";

const Searchbar = () => {
  return (
    <div className="w-[400px]">
      <Input placeholder="Search features, tutorials, etc." className="bg-black/5 border-0"/>
    </div>
  )
};

export default Searchbar;